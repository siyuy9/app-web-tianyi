{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "mkdir -p target && go build -o target/tianyi && target/tianyi server",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
